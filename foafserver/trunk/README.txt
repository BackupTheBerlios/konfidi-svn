
Dependencies:

Apache with mod_python
Python module: pyme 0.7.0 (a wrapper for gpgme)


NOTES:

pyme and/or mod_python and/or apache must be linked against the same version of libraries
If you incorrectly get "FOAFServerError: 'No PGP signature found'", recompile & install it all.